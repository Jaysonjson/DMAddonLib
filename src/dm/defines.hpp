#ifndef DEFINES_HPP
#define DEFINES_HPP

#include <string>
#include <vector>
#include <map>
#include <array>

using std::string;
using std::array;
using std::vector;
using std::map;

namespace DM {
    constexpr int CURRENT_PROTOCOL = 0;
}
#endif //DEFINES_HPP
